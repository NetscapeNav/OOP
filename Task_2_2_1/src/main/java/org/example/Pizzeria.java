package org.example;

import java.util.Queue;

public class Pizzeria<T> {
    /*private final json config;
    private final int orderCount;
    private final int bakerCount;
    private final int deliveryCount;
    private final BlockingQueue<Object> bakerQueue;
    private final BlockingQueue<T> deliveryQueue;

    public Pizzeria(json config, int orderCount, int bakerCount, int deliveryCount) {
        this.config = config;
        this.orderCount = orderCount;
        this.bakerCount = bakerCount;
        this.deliveryCount = deliveryCount;
        this.bakerQueue = new BlockingQueue<>(bakerCount);
        this.deliveryQueue = new BlockingQueue<>(deliveryCount);

        // нужны какие-то start
    }*/


}
